package de.lars.utilsManager.entitys;

public class NPCs {

    public void summonNPCs() {
        /*NPCPool npcPool = new NPCPool(Main.getInstance());
        Profile profile = new Profile("BastiGHG");
        SpawnCustomizer spawnCustomizer = new SpawnCustomizer() {
            @Override
            public void handleSpawn(NPC npc, Player player) {

            }
        };
        NPC.builder().location(new Location(world, -74.5, 67,4.5)).imitatePlayer(true).lookAtPlayer(false).usePlayerProfiles(true).profile(profile).spawnCustomizer(spawnCustomizer).build(npcPool);

         */
    }
}
